apiVersion: v1
kind: Service
metadata:
  name: find-my-hobby-web
  labels:
    app: find-my-hobby-web
spec:
  type: LoadBalancer
  selector:
    app: find-my-hobby-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
